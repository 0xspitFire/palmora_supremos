[extend]
useDefault = true

[allowlist]
description = "Known public blockchain addresses and test fixtures are not credentials"
regexes = [
  '''0x[0-9a-fA-F]{40}'''
]
paths = [
  '''pnpm-lock\.yaml''',
  '''docs/fixtures/'''
]
